<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  shit daemon LaunchAgent template.
  Substituted at install time:
    @@LABEL@@        — reverse-DNS label, e.g. com.tenseleyflow.shit
    @@SHITD_BIN@@    — absolute path to the shitd binary
    @@CONFIG_PATH@@  — absolute path to the shitd config file
    @@LOG_PATH@@     — absolute path for stdout/stderr capture
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>@@LABEL@@</string>

    <key>ProgramArguments</key>
    <array>
        <string>@@SHITD_BIN@@</string>
        <string>--foreground</string>
        <string>--config</string>
        <string>@@CONFIG_PATH@@</string>
    </array>

    <key>RunAtLoad</key>
    <true/>

    <!--
      KeepAlive: restart on any non-zero exit. Clean exit (idle-down) does
      not trigger restart; clients spawn the daemon again on next IPC
      connect via `shit service start`. Service-managed installs typically
      set idle_timeout_secs = 0 in the config to pin the daemon up.
    -->
    <key>KeepAlive</key>
    <dict>
        <key>SuccessfulExit</key>
        <false/>
        <key>Crashed</key>
        <true/>
    </dict>

    <key>ThrottleInterval</key>
    <integer>10</integer>

    <key>ProcessType</key>
    <string>Background</string>

    <key>StandardOutPath</key>
    <string>@@LOG_PATH@@</string>
    <key>StandardErrorPath</key>
    <string>@@LOG_PATH@@</string>
</dict>
</plist>
